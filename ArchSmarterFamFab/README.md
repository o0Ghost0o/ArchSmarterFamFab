## Future Improvements

- **Parametric dimensions** — Automatically create dimension strings labeled with family parameters so geometry is driven by parameter values, eliminating the need to manually add dimensions in the Family Editor
