<Window x:Class="ArchSmarterFamFab.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FamFab Settings"
        Width="480"
        Height="380"
        MinWidth="360"
        MinHeight="320"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        FontFamily="Segoe UI">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="6"
                      CornerRadius="8" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="FamFabStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="#3f3f46"
            BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Border Background="#1e1e1e" CornerRadius="8,8,0,0"
                    Padding="16,10" MouseDown="TitleBar_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold"
                                   Foreground="#e0e0e0" VerticalAlignment="Center">
                            <Run Text="Fam"/><Run Text="|" Foreground="#E8812A"/><Run Text="Fab"/>
                        </TextBlock>
                        <TextBlock Text="&#x00B7;" FontSize="13"
                                   Foreground="#3f3f46" VerticalAlignment="Center"
                                   Margin="10,0"/>
                        <TextBlock Text="Settings" FontSize="11"
                                   Foreground="#808080" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Click="BtnClose_Click"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="closeBg" Background="Transparent"
                                        CornerRadius="4" Padding="8,4">
                                    <TextBlock Text="&#x2715;" FontSize="11"
                                               Foreground="#808080"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="closeBg" Property="Background"
                                                Value="#3f3f46"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="32,24,32,32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Provider -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="Provider" Margin="0,0,0,6" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding ProviderOptions}"
                              SelectedItem="{Binding SelectedProviderDisplay, Mode=TwoWay}"/>
                </StackPanel>

                <!-- API Key -->
                <StackPanel Grid.Row="2">
                    <TextBlock Text="{Binding KeyLabel}" Margin="0,0,0,6" FontWeight="SemiBold"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <PasswordBox x:Name="ApiKeyPasswordBox" Grid.Column="0"
                                     PasswordChanged="ApiKeyPasswordBox_PasswordChanged"/>
                        <TextBox x:Name="ApiKeyTextBox" Grid.Column="0"
                                 Text="{Binding ApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Visibility="Collapsed"/>
                        <Button x:Name="BtnToggleKey" Grid.Column="1" Content="Show"
                                Style="{StaticResource GhostButton}"
                                Margin="8,0,0,0"
                                Click="BtnToggleKey_Click"/>
                    </Grid>
                </StackPanel>

                <!-- Model -->
                <StackPanel Grid.Row="4">
                    <TextBlock Text="Model" Margin="0,0,0,6" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding AvailableModels}"
                              SelectedItem="{Binding SelectedModel}"/>
                </StackPanel>

                <!-- Status -->
                <StackPanel Grid.Row="6">
                    <TextBlock Text="{Binding StatusText}"
                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Close button -->
                <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Close"
                            Style="{StaticResource PrimaryButton}"
                            Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
